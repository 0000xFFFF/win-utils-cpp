#include <iostream>
#include <string>
#include <direct.h>
#include <windows.h>

int main() {
	
	// //direct.h
	// chdir(setPath);                            //CHANGE DIR
	// std::cout << getcwd(NULL, 0) << std::endl; //PRINT PWD
	
	// //windows.h
	// SetCurrentDirectory(setPath); //CHANGE DIR
	
	//PRINT PWD
	// char buff[MAX_PATH + 1];
	// GetCurrentDirectory(MAX_PATH, buff);
	// buff[MAX_PATH + 1] = '\0';
	// std::cout << buff << std::endl;
	
	return 0;
}
